# TODO

- Remove hidden legacy command paths once users have migrated to namespace/action commands and bundle-name runtime operations:
  - top-level legacy env aliases: `set`, `import`, `delete`, `unset`, `unset-from`, `reveal`
  - top-level legacy profile aliases: `init`, `reset`
  - hidden profile runtime commands: `profile run`, `profile rollback`
